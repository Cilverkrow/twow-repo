This directory records a safe preflight abort.

The retry did not proceed because the exact Windows PowerShell 5.1 host reported Administrator=False. The approved procedure requires Administrator=True before MariaDB startup.

The current mangosd.conf has the expected acknowledged-state candidate identity and effective AutoDonationPoints values, but the user has not yet explicitly confirmed that this save was intentional. No alternate local copy was available for a meaningful content diff.

No database process was started. No backup, SQL statement, migration, schema change, runtime test, Worldserver start, or Realmserver start occurred.
