# Canonical non-secret Compose overrides for aiplayerbot.conf.
# Later keys override the complete tracked base template.
AiPlayerbot.Enabled = 1
AiPlayerbot.MinRandomBots = @AIPLAYERBOT_MIN_BOTS@
AiPlayerbot.MaxRandomBots = @AIPLAYERBOT_MAX_BOTS@
