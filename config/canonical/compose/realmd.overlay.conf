# Canonical non-secret Compose overrides for realmd.conf.
# Later keys override the complete tracked base template.
LogsDir = "/var/log/turtle/"
PatchesDir = "/opt/turtle/patches"
# Compose publishes the machine-local REALM_PORT to this fixed container port.
RealmServerPort = 3724
PidFile = "/tmp/twrealmd.pid"
